#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a,b,c,soma;
    printf("Digite a number (use . to decimals): ");
    scanf("%f", &a);
    printf("Digite a number (use . to decimals: ");
    scanf("%f", &b);
    printf (Digite a number (use . to decimals: ");
    scanf("%f", &c);
    soma = a+b+c;
    printf("The result of the three numbers sum is: %f", soma);
    return(0);
}
